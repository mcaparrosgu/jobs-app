# ═══════════════════════════════════════════════════════════════
#  PLANTILLA DE VARIABLES DE ENTORNO — Jobs App
# ═══════════════════════════════════════════════════════════════
#
#  Este archivo SÍ se sube a git: solo tiene los NOMBRES de las
#  claves, nunca los valores.
#
#  Para trabajar en tu ordenador:
#    1. Copia este archivo y llámalo  .env.local
#    2. Rellena los valores reales ahí
#    3. .env.local NUNCA se sube (ya está en .gitignore)
#
#  ⚠ Todo lo que empieza por NEXT_PUBLIC_ viaja al navegador y
#    CUALQUIERA puede leerlo. Es como escribirlo en el escaparate.
#    Solo pueden llevar ese prefijo las claves que no importan.
#
# ═══════════════════════════════════════════════════════════════


# ─── Supabase (base de datos y acceso por email) ───────────────
#
# Dónde se sacan: supabase.com → tu proyecto → Project Settings
#                 → API
#
# Dónde van los valores reales en producción:
#   Vercel → tu proyecto → Settings → Environment Variables

# Dirección de tu base de datos. No es secreta.
NEXT_PUBLIC_SUPABASE_URL=

# Clave de acceso público. No es secreta: lo que impide que alguien
# vea datos ajenos con ella es RLS, la regla de la base de datos que
# hace que cada ficha solo se entregue a su dueña.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 🔴 SECRETA. Acceso total, se salta RLS.
#    Jamás en un archivo de app/ que vea el navegador.
#    Jamás con el prefijo NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=


# ─── OpenRouter (respaldo del modelo de IA) ────────────────────
#
# Dónde se saca: openrouter.ai → Keys
#
# ⚠️ Desde el 20/08/2026 es el RESPALDO, no el principal: sus modelos
# gratis podían entrenar con las peticiones (o sea, con los CVs) y esa
# opción se apagó — ver knowledge/decision-groq-principal-privacidad.md.
# Al apagarla, OpenRouter deja de servir los modelos ":free", así que en
# la práctica casi nunca responderá. Se mantiene por si Groq retira su
# modelo sin aviso.
#
# Dónde va el valor real en producción:
#   Vercel → tu proyecto → Settings → Environment Variables

# 🔴 SECRETA. Quien la tenga consume tu cuota.
OPENROUTER_API_KEY=


# ─── Groq (el modelo de IA) ────────────────────────────────────
#
# Dónde se saca: console.groq.com → API Keys
#
# Es el proveedor PRINCIPAL desde el 20/08/2026, elegido por privacidad:
# tiene Zero Data Retention global activado, que es lo que corresponde
# cuando lo que viaja es el CV de otra persona.
#
# Cupo gratuito: 200.000 tokens al día (unos 30 documentos para las
# cinco usuarias) y 8.000 por minuto. Una tanda de evals consume una
# parte grande del día: no lanzarlos la mañana de una demo.
#
# Dónde va el valor real en producción:
#   Vercel → tu proyecto → Settings → Environment Variables

# 🔴 SECRETA. Quien la tenga consume tu cuota.
GROQ_API_KEY=


# ─── Gemini (principal solo de generarCvYCarta) ────────────────
#
# Dónde se saca: aistudio.google.com/apikey → Create API key
#
# Desde el 21/08/2026, PRINCIPAL solo de `generarCvYCarta` (no de
# `extraerPerfil`, que sigue en Groq): qwen3.6-27b de Groq salió inestable
# en esa llamada concreta en tres pasadas de evals seguidas — ver
# knowledge/decision-gemini-generarcv.md.
#
# Nivel gratuito, sin tarjeta. Google entrena con los prompts del nivel
# gratuito en general, PERO hay una excepción explícita en sus términos
# para usuarias del Espacio Económico Europeo (España incluida): a
# vosotras se os aplica el trato de "Paid Services" (sin entrenamiento)
# aunque no paguéis. No es Zero Data Retention real como en Groq (eso solo
# existe en el nivel de pago).
#
# Dónde va el valor real en producción:
#   Vercel → tu proyecto → Settings → Environment Variables

# 🔴 SECRETA. Quien la tenga consume tu cuota.
GEMINI_API_KEY=


# ═══════════════════════════════════════════════════════════════
#  CLAVES QUE NO VAN EN ESTE ARCHIVO
# ═══════════════════════════════════════════════════════════════
#
#  Contraseña SMTP de Gmail (para enviar el enlace de acceso)
#    → Supabase → Project Settings → Authentication → SMTP Settings
#
#  Credenciales de Gmail y de Apify (ingesta de ofertas y avisos)
#    → n8n → Credentials  (ya están ahí, no se tocan)
#
# ═══════════════════════════════════════════════════════════════
